# Backend Configuration
PORT=5000
JWT_SECRET=manchester_tech_secret_key_2026_default
PORTAL_URL=http://localhost:5173

# Email Configurations (Nodemailer)
# If left empty, mail alerts will be simulated and logged locally inside console streams.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
